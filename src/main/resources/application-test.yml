spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:taskmanager;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

app:
  jwt:
    secret: test-access-secret
    expiration-ms: 900000
    refresh-secret: test-refresh-secret
    refresh-expiration-ms: 604800000
